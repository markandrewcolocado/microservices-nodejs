apiVersion: v1
kind: Pod
metadata:
  name: posts
spec:
  containers:
    - name: posts
      image: markandrew05/posts # docker image name
      imagePullPolicy: Never # This will ensure that Kubernetes will use the image built locally
